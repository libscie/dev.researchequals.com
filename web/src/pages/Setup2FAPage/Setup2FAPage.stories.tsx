import type { Meta, StoryObj } from '@storybook/react'

import Setup2FaPage from './Setup2FaPage'

const meta: Meta<typeof Setup2FaPage> = {
  component: Setup2FaPage,
}

export default meta

type Story = StoryObj<typeof Setup2FaPage>

export const Primary: Story = {}
