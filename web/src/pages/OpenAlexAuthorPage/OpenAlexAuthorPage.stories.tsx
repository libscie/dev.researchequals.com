import type { Meta, StoryObj } from '@storybook/react'

import OpenAlexAuthorPage from './OpenAlexAuthorPage'

const meta: Meta<typeof OpenAlexAuthorPage> = {
  component: OpenAlexAuthorPage,
}

export default meta

type Story = StoryObj<typeof OpenAlexAuthorPage>

export const Primary: Story = {}
