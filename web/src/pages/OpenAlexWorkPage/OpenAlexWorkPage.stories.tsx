import type { Meta, StoryObj } from '@storybook/react'

import OpenAlexWorkPage from './OpenAlexWorkPage'

const meta: Meta<typeof OpenAlexWorkPage> = {
  component: OpenAlexWorkPage,
}

export default meta

type Story = StoryObj<typeof OpenAlexWorkPage>

export const Primary: Story = {}
